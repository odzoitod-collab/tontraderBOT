# 🚀 Инструкция по деплою на bothost.ru

## 📋 Что нужно:

1. **Аккаунт на bothost.ru**
2. **Токен бота** (уже есть в bot.py)
3. **Supabase проект** (уже настроен)
4. **Vercel сайт** (уже задеплоен на https://tontrade-web.vercel.app)

## 🔧 Настройка на bothost.ru

### Шаг 1: Создание проекта

1. Зайдите на https://bothost.ru
2. Создайте новый проект Python
3. Выберите Python 3.10+

### Шаг 2: Загрузка файлов

Загрузите следующие файлы:
- `bot.py` - основной файл бота
- `requirements.txt` - зависимости

### Шаг 3: Установка зависимостей

В `requirements.txt` должно быть:

```txt
aiogram==3.4.1
aiohttp==3.9.1
supabase==2.3.0
```

### Шаг 4: Настройка переменных окружения (опционально)

Если хотите использовать переменные окружения вместо хардкода:

```bash
BOT_TOKEN=7769124785:AAE46Zt6jh9IPVt4IB4u0j8kgEVg2NpSYa0
SUPABASE_URL=https://wzpywfedbowlosmvecos.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
WEBAPP_URL=https://tontrade-web.vercel.app
ADMIN_ID=844012884
```

### Шаг 5: Команда запуска

```bash
python3 bot.py
```

## 🌐 Архитектура системы

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Пользователь открывает сайт через Telegram WebApp     │
│  https://tontrade-web.vercel.app                        │
│                                                         │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │                       │
         │   Supabase Database   │
         │   (Центральная БД)    │
         │                       │
         │  • users              │
         │  • trades             │
         │  • deposit_requests   │
         │  • settings           │
         │                       │
         └───────┬───────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
┌───────────────┐  ┌──────────────┐
│               │  │              │
│  Vercel Site  │  │  Bot (Host)  │
│               │  │              │
│  • React App  │  │  • Telegram  │
│  • Realtime   │  │  • Realtime  │
│  • Trading    │  │  • Worker    │
│               │  │    Panel     │
└───────────────┘  └──────────────┘
```

## 🔄 Как работает взаимодействие:

### 1. Пополнение баланса:

```
Пользователь (Сайт)
    ↓
Создает deposit_request в Supabase
    ↓
Supabase Realtime уведомляет Бота
    ↓
Бот отправляет сообщение Воркеру с кнопками
    ↓
Воркер нажимает "Подтвердить"
    ↓
Бот обновляет deposit_request.status = 'approved'
    ↓
Supabase Realtime уведомляет Сайт
    ↓
Баланс пользователя обновляется автоматически
```

### 2. Открытие сделки:

```
Пользователь открывает сделку на сайте
    ↓
Сделка сохраняется в Supabase (trades)
    ↓
Цена рассчитывается локально на сайте
    ↓
При закрытии результат сохраняется в БД
```

### 3. Настройки (Support, Реквизиты):

```
Админ меняет настройки в боте (/admin)
    ↓
Бот обновляет settings в Supabase
    ↓
Supabase Realtime уведомляет Сайт
    ↓
Настройки обновляются на сайте автоматически
```

## ✅ Преимущества такой архитектуры:

1. **Независимость** - Сайт и бот работают независимо
2. **Надежность** - Если бот упадет, сайт продолжит работать
3. **Масштабируемость** - Можно добавить несколько ботов
4. **Realtime** - Все обновления мгновенные через Supabase
5. **Простота** - Не нужны сложные API между ботом и сайтом

## 🔍 Проверка работы:

### После деплоя проверьте:

1. **Бот запустился:**
```bash
# В логах должно быть:
🚀 Бот запущен!
✅ Subscribed to deposit_requests realtime updates
🌐 API сервер запущен на порту 8080
```

2. **Команды работают:**
- `/start` - приветствие
- `/worker` - панель воркера
- `/admin` - админ панель

3. **Realtime работает:**
- Создайте запрос на пополнение на сайте
- Воркеру должно прийти уведомление в бота

4. **Настройки синхронизируются:**
- Измените Support в боте
- Проверьте что на сайте обновилось

## 🐛 Отладка:

### Если бот не получает уведомления о пополнениях:

1. Проверьте что Realtime включен в Supabase:
```sql
SELECT * FROM pg_publication_tables WHERE pubname = 'supabase_realtime';
```

2. Проверьте логи бота на bothost.ru

3. Проверьте что deposit_requests создаются:
```sql
SELECT * FROM deposit_requests ORDER BY created_at DESC LIMIT 5;
```

### Если сайт не обновляется:

1. Проверьте консоль браузера (F12)
2. Проверьте что Realtime подключен в Supabase Dashboard
3. Проверьте что политики RLS настроены правильно

## 📞 Поддержка:

Если что-то не работает:

1. Проверьте логи бота на bothost.ru
2. Проверьте логи Vercel (https://vercel.com/dashboard)
3. Проверьте Supabase Dashboard (https://supabase.com/dashboard)
4. Проверьте что все токены и ключи правильные

## 🎯 Важные URL:

- **Сайт:** https://tontrade-web.vercel.app
- **Supabase:** https://wzpywfedbowlosmvecos.supabase.co
- **Бот:** @YourBotUsername (замените на свой)

## 🔐 Безопасность:

⚠️ **ВАЖНО:** Не публикуйте в открытый доступ:
- BOT_TOKEN
- SUPABASE_KEY
- ADMIN_ID

Эти данные должны быть только в приватных репозиториях или переменных окружения!
