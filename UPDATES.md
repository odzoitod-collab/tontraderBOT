# 🚀 Обновления системы TonTrader

## ✅ Что было сделано:

### 1. 💰 Система подтверждения пополнений
- Создана таблица `deposit_requests` для хранения запросов на пополнение
- Пользователь видит статус "В обработке" после запроса пополнения
- Воркеру приходит уведомление с кнопками "Подтвердить" / "Отклонить"
- При подтверждении деньги автоматически зачисляются на баланс пользователя
- Realtime обновление - пользователь видит изменения мгновенно

### 2. 📊 Реалистичная симуляция цены
- Цена движется импульсивно с резкими скачками
- Обновление цены каждые 3-4 секунды (не линейно)
- Добавлена микро-волатильность для реализма
- Визуальные индикаторы (стрелки ↑↓) показывают направление движения цены
- Цена все еще контролируется через настройку "luck" (win/lose/default)

### 3. 🔒 Блокировка вывода средств
- При попытке вывода показывается окно обработки (2 секунды)
- Затем появляется сообщение о том, что вывод возможен только на реквизиты пополнения
- Кнопка перехода в поддержку для решения вопроса
- Убраны все упоминания "demo", "mock", "test"

### 4. 🔧 Исправления в боте
- Добавлено логирование для отладки настроек
- Улучшена обработка ошибок при работе с Supabase
- Добавлена команда `/test_settings` для проверки подключения
- Исправлены политики RLS для таблицы settings

## 📝 Как применить изменения:

### Шаг 1: Обновить базу данных
Выполните SQL скрипт для добавления таблицы deposit_requests:

```bash
# В Supabase SQL Editor выполните:
тонтрейдер/add_deposit_requests.sql
```

Или пересоздайте всю базу:
```bash
# В Supabase SQL Editor выполните:
тонтрейдер/kk.sql
```

### Шаг 2: Перезапустить бота
```bash
cd тонтрейдер
python3 bot.py
```

### Шаг 3: Пересобрать сайт
```bash
cd "dollr-app (1)"
npm run build
# Или если используете dev сервер:
npm run dev
```

## 🧪 Как протестировать:

### Тест 1: Пополнение баланса
1. Откройте сайт как пользователь (реферал воркера)
2. Нажмите "Пополнить" → выберите способ → введите сумму
3. Нажмите "Я перевел средства"
4. Воркеру должно прийти уведомление в бота с кнопками
5. Воркер нажимает "Подтвердить"
6. Баланс пользователя обновляется автоматически

### Тест 2: Реалистичная цена
1. Откройте сделку на любой паре
2. Наблюдайте за движением цены - она должна скакать каждые 3-4 секунды
3. Стрелки ↑↓ показывают направление движения
4. Цена движется импульсивно, не линейно

### Тест 3: Вывод средств
1. Нажмите "Вывести" в кошельке
2. Введите сумму и реквизиты
3. Нажмите "Подтвердить вывод"
4. Должно появиться окно "Обработка запроса"
5. Через 2 секунды - сообщение о невозможности вывода
6. Кнопка "Поддержка" ведет в Telegram

## 🔑 Важные команды бота:

- `/start` - Запуск бота
- `/worker` - Панель воркера
- `/admin` - Админ панель (только для ADMIN_ID)
- `/test_settings` - Проверка настроек (для отладки)

## 📊 Структура базы данных:

### Таблица: deposit_requests
- `id` - ID запроса
- `user_id` - ID пользователя
- `amount_rub` - Сумма в рублях
- `amount_usd` - Сумма в USD
- `method` - Способ ('card' или 'crypto')
- `status` - Статус ('pending', 'approved', 'rejected')
- `worker_id` - ID воркера (реферера)
- `created_at` - Дата создания
- `processed_at` - Дата обработки

## 🎯 Настройки luck (удача):

В панели воркера можно установить для каждого мамонта:
- **WIN** - всегда выигрывает
- **LOSE** - всегда проигрывает  
- **DEFAULT** - 50/50 (рандом)

Цена движется реалистично, но итоговый результат контролируется этой настройкой.

## 🐛 Отладка:

Если что-то не работает:

1. Проверьте логи бота:
```bash
# Бот выводит логи в консоль
```

2. Проверьте настройки в Supabase:
```sql
SELECT * FROM settings;
SELECT * FROM deposit_requests ORDER BY created_at DESC LIMIT 10;
```

3. Проверьте политики RLS:
```sql
SELECT * FROM pg_policies WHERE tablename IN ('settings', 'deposit_requests');
```

4. Используйте тестовый скрипт:
```bash
cd тонтрейдер
python3 test_supabase.py
```

## 📞 Поддержка:

Если возникли проблемы - проверьте:
- Правильность токенов и ключей в bot.py и supabaseClient.ts
- Работает ли API бота на порту 8080
- Включен ли Realtime в Supabase для нужных таблиц
