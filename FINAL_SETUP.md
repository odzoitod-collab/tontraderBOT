# ✅ Финальная настройка системы

## 🎯 Текущее состояние:

- ✅ Сайт задеплоен на Vercel: https://tontrade-web.vercel.app
- ✅ Supabase настроен и работает
- ✅ Бот готов к деплою на bothost.ru
- ✅ Все взаимодействие через Supabase (без локальных API)

## 📝 Что нужно сделать:

### 1. Применить изменения в Supabase

Выполните SQL скрипт для добавления таблицы deposit_requests:

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите проект `wzpywfedbowlosmvecos`
3. Перейдите в SQL Editor
4. Выполните скрипт: `add_deposit_requests.sql`

Или выполните вручную:

```sql
-- Создаем таблицу deposit_requests
CREATE TABLE IF NOT EXISTS public.deposit_requests (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL REFERENCES public.users(user_id),
    amount_rub FLOAT NOT NULL,
    amount_usd FLOAT NOT NULL,
    method TEXT NOT NULL,
    status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
    worker_id BIGINT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
    processed_at TIMESTAMP WITH TIME ZONE
);

-- Индексы
CREATE INDEX IF NOT EXISTS idx_deposit_requests_worker ON public.deposit_requests(worker_id, status);
CREATE INDEX IF NOT EXISTS idx_deposit_requests_user ON public.deposit_requests(user_id, status);

-- RLS
ALTER TABLE public.deposit_requests ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Enable all for deposit_requests" ON public.deposit_requests FOR ALL USING (true) WITH CHECK (true);

-- Realtime
DROP PUBLICATION IF EXISTS supabase_realtime;
CREATE PUBLICATION supabase_realtime FOR TABLE public.users, public.trades, public.settings, public.deposit_requests;
```

### 2. Включить Realtime в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в Database → Replication
3. Убедитесь что включены таблицы:
   - ✅ users
   - ✅ trades
   - ✅ settings
   - ✅ deposit_requests

### 3. Задеплоить бота на bothost.ru

1. Зайдите на https://bothost.ru
2. Создайте новый проект Python 3.10+
3. Загрузите файлы:
   - `bot.py`
   - `requirements.txt`
4. Установите зависимости (автоматически)
5. Запустите: `python3 bot.py`

### 4. Проверить работу системы

#### Тест 1: Бот запустился
```bash
# В логах bothost.ru должно быть:
🚀 Бот запущен!
✅ Subscribed to deposit_requests realtime updates
🌐 API сервер запущен на порту 8080
```

#### Тест 2: Команды работают
- Отправьте `/start` боту
- Отправьте `/worker` боту
- Отправьте `/admin` боту (если вы админ)

#### Тест 3: Сайт открывается
- Откройте https://tontrade-web.vercel.app
- Или откройте через бота (кнопка "Открыть TonTrader")

#### Тест 4: Пополнение работает
1. Откройте сайт как реферал воркера
2. Нажмите "Пополнить" → выберите способ → введите сумму
3. Нажмите "Я перевел средства"
4. Воркеру должно прийти уведомление в бота
5. Воркер нажимает "Подтвердить"
6. Баланс обновляется на сайте автоматически

#### Тест 5: Настройки синхронизируются
1. Админ меняет Support в боте (`/admin`)
2. Проверьте что на сайте обновилось (в кошельке или профиле)

## 🔧 Настройка URL бота в Telegram

После деплоя на bothost.ru, обновите URL в боте:

1. Найдите ваш бот в @BotFather
2. Отправьте `/setmenubutton`
3. Выберите вашего бота
4. Отправьте URL: `https://tontrade-web.vercel.app`

Или используйте команду:
```
/setmenubutton
@YourBotUsername
https://tontrade-web.vercel.app
```

## 📊 Мониторинг

### Логи бота (bothost.ru):
- Проверяйте регулярно на наличие ошибок
- Особенно важны сообщения о Realtime подписках

### Логи Vercel:
- https://vercel.com/dashboard
- Проверяйте ошибки в консоли браузера (F12)

### Supabase Dashboard:
- https://supabase.com/dashboard
- Проверяйте Table Editor для просмотра данных
- Проверяйте Logs для отладки

## 🎨 Кастомизация

### Изменить название бота:
В `bot.py` найдите приветственное сообщение и измените текст.

### Изменить курс RUB/USD:
В `App.tsx` найдите:
```typescript
const amountUsd = amount * 0.0105; // 1 RUB = 0.0105 USD (курс ~95)
```

### Изменить комиссию вывода:
В `WalletPage.tsx` найдите:
```typescript
<div className="text-xs text-gray-500 mt-1">Комиссия: 1 USDT</div>
```

## 🚨 Важные моменты:

1. **Не удаляйте API сервер из бота** - он нужен для health check
2. **Проверьте что Realtime включен** в Supabase для всех таблиц
3. **Убедитесь что политики RLS настроены** правильно
4. **Проверьте что WEBAPP_URL** в bot.py указывает на Vercel

## 🎉 Готово!

После выполнения всех шагов система полностью готова к работе:

- ✅ Сайт на Vercel
- ✅ Бот на bothost.ru
- ✅ База данных в Supabase
- ✅ Realtime синхронизация
- ✅ Система пополнений
- ✅ Реалистичный трейдинг
- ✅ Блокировка вывода

Пользователи могут начинать торговать! 🚀
